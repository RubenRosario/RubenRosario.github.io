import React from 'react';
import { CSSTransition } from 'react-transition-group';

import './AboutMe.css';

const AboutMe = (props) => {
  return <div> About me page is so hot!</div>;
};

export default AboutMe;
